# Code review triage — dogfood #2 slice (NER-386 typed blame codes + NER-387 --at)

- **Date:** 2026-07-10
- **Branch:** `experiment/ccx-dogfood2` (local only)
- **base-sha:** `4429101d7e1b60a991429b7bace343e98606fbe7` (merged main: NER-362 + NER-382)
- **head-sha (reviewed):** `3e78a82e168b921b2a4cb39ce4f0dd56ca6c72d5` + the two uncommitted agent patches; review fixes land in the follow-up `fix(review)` commit
- **Gate:** `/ce-code-review` — 8 reviewer passes (correctness, adversarial, **security** [secret-redaction path touched] at session model; testing, maintainability, project-standards, api-contract, agent-native+learnings at mid-tier) + Codex cross-model adversarial pass + 2-validator wave + orchestrator direct verification. CI mirror was green pre-review.
- **Verdict:** Ready with fixes (all applied same-session; one deferred ticket).

## Real-actionable (fixed in the `fix(review)` commit)

1. **`--at` masked ledger-referenced corruption as "unknown commit"** — `core.rs` --at arm did parse + kind-check + bare `read_commit().is_err()` bail while its own comment claimed checkout parity; checkout's fallback queries the ledger and returns `NATIVE_HISTORY_CORRUPT{DanglingCommitId}` for referenced ids. Three-reviewer agreement (correctness 75, adversarial 75, maintainability 100 — also flagged as copy-paste divergence); validator CONFIRMED. Fixed: extracted `classify_missing_commit(cwd, id)` into forge-store `snapshots.rs` (facade re-export), checkout refactored onto it (behavior-preserving), blame `--at` uses it. Parity test added (ledger-referenced-but-missing → `NATIVE_HISTORY_CORRUPT`).
2. **Three residual path-leaking error sites in `attribute_lines_at`** — the walk-lost-path bail, unsupported-change bail, and unattributed-line `with_context` interpolated the user path into un-redacted `message`/stderr via `COMMAND_FAILED` fallthrough, contradicting NER-386's own path-free invariant. Security 75 + correctness 50 + adversarial 50; orchestrator direct-verified (quotes entail). Fixed: new path-free `ProvenanceError::WalkInconsistent{commit_id, reason}` variant replaces all three; path-free-Display unit test covers every reason.
3. **Two unmapped `diff_native_trees` call sites** — mid-walk dangling subtree/blob escaped as `COMMAND_FAILED` instead of `NATIVE_HISTORY_CORRUPT` (rename detection reads blobs even with hunks off). Adversarial 75; validator CONFIRMED (including that the sites are new in this diff). Fixed: both mapped to the `StoreCorrupt`/`DanglingTree` classification like sibling dereferences.
4. **Test gaps closed** (testing 100/75 + security + correctness + adversarial gaps + project-standards note): corrupt-store → `NATIVE_HISTORY_CORRUPT` CLI bridge test; `--at` with a well-formed tree id; CLI-level `.env` redaction assertion (`details.path == "[secret-risk path redacted]"`, message path-free); `--at`+`PATH_NOT_FOUND` with `details.tip` = the --at commit. forge_blame.rs now 22 tests.

## Defer-able (filed in Forge Linear)

- **NER-388** — shared typed `UNKNOWN_COMMIT` code for checkout + blame `--at` (currently both use the untyped path-free "unknown commit" → `COMMAND_FAILED` convention; the 387 contract deliberately forbade minting a blame-only code).

## Defense-in-depth / residual (recorded, no action)

- `--at` accepts any store-present commit even if unreachable from this repo's ledger history (peer-imported/orphaned objects); acceptance predicate is store-presence.
- Tiny TOCTOU window between `--at` pre-flight and the walk's re-read (read-only, gc heavily gated).
- `ObjectId::parse` failures on stored fields inside the walk surface as `COMMAND_FAILED` (malformed stored ids, distinct from missing objects).
- `blob_lines` labels a missing blob as `DanglingTree` (no `DanglingBlob` kind exists).
- Downcast-through-anyhow mapping would silently degrade if a future `map_err` rewrap (not `.context`) intervenes.
- `core.rs` ~2400 lines and growing toward the 3000 ceiling — blame extraction candidate.
- Policy-excluded paths in peer-synced trees would silently attribute to genesis (unconfirmed producible; carried from dogfood #1).

## Reviewed-and-rejected / known noise

- Dogfood #1 carryovers stand: cyclic-history hang (content addressing) and rename misattribution (unit-tested) — neither re-flagged by any reviewer this round, as directed by the learnings pass.
- Blame's `COMMAND_FAILED` → typed-code transition is a machine-visible behavior change but is NER-386's sanctioned purpose (api-contract: not an additive-only violation).

## Coverage notes

- Codex cross-model pass: **zero findings, zero residuals** — first fully clean cross-model pass in the experiment.
- Validators: 2 dispatched, 2 confirmed. Orchestrator direct verification used for the mechanical path-leak finding (quotes entail conclusion).
- Learnings pass correctly framed this slice as the direct resolution of dogfood #1's two deferred tickets and pre-marked the known-noise items; the typed-error-contract solution doc was cited as precedent for the path-leak/COMMAND_FAILED findings.
- Run-record artifacts scanned for secrets by two reviewers: clean.
