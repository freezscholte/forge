# Code review triage — NER-362 dogfood slice (blame + harness fixes + contracts)

- **Date:** 2026-07-07
- **Branch:** `experiment/ccx-spikes` (local only)
- **base-sha:** `d6e89a98754dd70e4e88a17962506acf57147840`
- **head-sha:** `d13dbb62574f889969ad0c21fb4d008087b4431c` (reviewed head; review fixes land in the follow-up `fix(review)` commit)
- **Gate:** `/ce-code-review` mode:agent — 9 personas (correctness + adversarial at session model, 7 mid-tier) + independent cross-model adversarial pass via Codex + 6-validator verification wave. Verify trio, `scripts/ci.sh` (incl. 95/95 e2e), and per-task ccx fix/guard sets were green before the review.
- **Verdict:** Ready with fixes (all applied same-session; two deferred as tickets).

## Real-actionable (fixed in the `fix(review)` commit)

1. **Quadratic LCS memory in `match_lines`** — `crates/forge-content-native/src/provenance.rs:261`. Uncapped `O(parent_mid * child_mid)` table per walk step; a substantially rewritten large file could OOM `forge blame`. Flagged independently by adversarial (75) + correctness (50), echoed by Codex; validator CONFIRMED (no size guard anywhere in the crate). Fixed: `LCS_CELL_LIMIT` (64M cells) — past the bound the trimmed middle degrades to child-commit attribution (same degrade-not-fail convention as `rename_limit`), with two unit tests (degrade path + prefix/suffix still match past the limit).
2. **Non-firing `||` guards on path absolutization + triplicated one-liner** — `tools/ccx/lib.sh:71`, `verify-task.sh:48`, (`run-task.sh --out` variant). An assignment's exit status is its LAST command substitution's (`basename`), so a failed `cd` silently yielded `/<name>`. Maintainability (75) + adversarial (50); validator CONFIRMED both the duplication and the bash semantics. Fixed: single `ccx_abspath()` helper in `lib.sh` with the failure handled inside; both call sites converted; harness suite green.
3. **Short-commit-digest computation duplicated in blame's human arm** — `crates/forge-cli/src/commands/core.rs`. Maintainability (100, mechanical). Fixed: hoisted `short_digest` closure.
4. **Intent-less (sync-merge) enrichment skip branch untested** — `crates/forge-cli/src/commands/core.rs:1070`. Sync-merge commits carry `intent_id: None` (sync.rs) and no test drove blame over one. Testing (75); validator CONFIRMED. Fixed: integration test added to `forge_blame.rs` (sync-merge scenario; JSON nulls + human legend `-`).

## Defer-able (filed in Forge Linear)

- **NER-386** — blame domain failures (no history / missing path / binary blob) fall to generic `COMMAND_FAILED`; sibling `log` uses typed `NATIVE_HISTORY_CORRUPT`. api-contract (75), validator CONFIRMED. Deferred: new error codes are contract-update-required scope. Folds in the defense-in-depth walker/typed-corruption asymmetry.
- **NER-387** — `forge blame --at <commit_id>` read-only historical selector (agent-native should-fix; primitives already support it, CLI pins the tip per the 362-3 contract).

## Defense-in-depth / residual (recorded, no action this slice)

- Blame tip vs a checked-out older worktree silently differ (advisory: consider an envelope warning when the current view's content_ref ≠ tip tree; schema documents only the HEAD-lag window).
- Genesis fallback could misattribute policy-filtered paths in peer-imported trees (adversarial, anchor 50, uncorroborated).
- `provenance_detail` reopens a repository context + SQLite connection per distinct commit tuple (O(distinct commits) as contracted; churn on long histories). Blame walks tip→genesis twice (path_provenance + attribute_lines).
- `forge-content-native/src/lib.rs` sits at exactly its 4730-line cap; next facade change invites comment-squeezing.
- Untested edges: `rename_detection_skipped` (rename-limit) degradation; empty committed file blame; legend tie ordering; ccx paths with spaces/symlinks; `--out` inside the clone (wiped by rebuild's `git clean`, fails loudly but isn't refused at parse time).
- Handoff doc embeds the operator's absolute local path; README documents blame as a bullet, not a full section.

## Reviewed-and-rejected (do not re-flag)

- **"Cyclic native history hangs blame forever" (reliability P1)** — validator REJECTED: every commit read path (loose + packed) SHA-256-verifies bytes against the object id with no cache bypass, so a parent cycle requires a hash collision; a dangling parent errors loudly. The residual typed-error asymmetry is tracked in NER-386.
- **"Blame stops at rename commits" (cross-model P2)** — validator REJECTED: the `added` arm fires only for true adds; `R*` maps to `renamed` and retargets to `old_path`, proven by unit tests (`provenance_follows_the_old_name_back_through_a_rename`, `attribution_follows_renames_back_to_the_original_lines`). Only the rename-limit edge degrades (residual above).
- Carried over from `2026-07-06-ccx-thin-harness.md`: `test_blast.py` `SECRET=hunter2`/`.env` fixtures are intentional test inputs; `_scan_decl_line` leniency biases toward rejection (safe direction).

## Coverage notes

- Learnings pass matched 5 prior solutions; the ledger-tip design implements `commit-on-accept-ordering-ledger-authoritative-reconcile` exactly; prior harness review's 10 fixes verified intact.
- Cross-model pass ran (Codex, read-only): 1 finding (rejected on validation), 2 residuals (one echoed the confirmed LCS issue).
- Suppressed at anchor 50 by the confidence gate: 3 (routed to residuals above). Validators: 6 dispatched, 4 confirmed, 2 rejected, 0 infra failures. Repo-profile cache: MISS, skipped (optimization only). Review range includes the pre-existing handoff-doc commit `0b86fbb`.
